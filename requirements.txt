# Referencia pip (por si alguien no usa conda). El entorno "oficial" del
# proyecto es environment.yml, según lo pactado con el cliente (T01-T03).
Django>=5.1,<5.2
psycopg2-binary>=2.9
Pillow>=10.0
django-environ>=0.11
whitenoise>=6.6
reportlab>=4.0
